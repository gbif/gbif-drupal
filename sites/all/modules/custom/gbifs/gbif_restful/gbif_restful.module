<?php

/**
 * @file
 * Example module for the RESTful module.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function gbif_restful_ctools_plugin_directory($module, $plugin) {
  if ($module == 'restful') {
    return 'plugins/' . $plugin;
  }
}
